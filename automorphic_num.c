//number whose square ends with the number itself
#include<stdio.h>
int isAutomorphic(int num)
{   int square=num*num;
    rev=rev*10



    
    while(square%10!=num)
    {
        square=square%10;

    }
    
}
int main()
{
    int num;
    scanf("%d",&num);
}